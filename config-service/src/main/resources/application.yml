server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/native-file-repo
        git:
#          uri: file:///Users/limjongwoo/git-local-repo
          uri: https://github.com/jongwoolim/spring-cloud-config
#        uesrname: [your username] private일 경우
#        password: [your password] private일 경우